//
//  AlertHelper.swift
//  DropWallet
//
//  Created by Richard Torcato on 2022-12-29.
//
import SwiftUI
import Foundation

public final class AlertHelper {
    /// Show a simple alert anywhere from code
    /// - Parameters:
    ///   - title: Alert title
    ///   - message: Alert message
    ///   - actionLabel: Label for default button
    /*
    public static func showSimpleAlert(title: String, message: String, actionLabel: String = "OK") {
//    #if os(iOS)
        let alertController = UIAlertController(
            title: title,
            message: message,
            preferredStyle: .alert
        )
        alertController.addAction(UIAlertAction(title: actionLabel, style: .default, handler: nil))
        UIApplication.shared.topViewController()?.present(alertController, animated: true, completion: nil)
        // MARK: Example
        // AlertHelper.showSimpleAlert(
        //    title: "Scanning Not Supported",
        //    message: "This device doesn't support smart tag scanning. \n Use iPhone X or newer"
        // )
//        #endif
    }     */
}


